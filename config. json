{
    // Конфигурационен обект за стартиране на дебъг сесии.
    "launch": {
        // Множество конфигурации за различни типове дебъгване.
        "configurations": [
            {
                // Тип на конфигурацията, в този случай за Node.js.
                "type": "node",
                // Режим на стартиране на дебъг сесията.
                "request": "launch",
                // Име на конфигурацията, което се показва в списъка с конфигурации.
                "name": "Launch Program",
                // Програмата, която ще бъде стартирана (текущият файл).
                "program": "${file}"
            },
            {
                // Тип на конфигурацията, в този случай за Python.
                "type": "python",
                // Режим на стартиране на дебъг сесията.
                "request": "launch",
                // Име на конфигурацията, което се показва в списъка с конфигурации.
                "name": "Launch Flask App",
                // Програмата, която ще бъде стартирана (Flask приложение).
                "program": "${workspaceFolder}/app.py",
                // Аргументи, които се подават към програмата при стартиране.
                "args": ["run"],
                // Среда на изпълнение, определяща различни променливи на средата.
                "env": {
                    // Определя кой файл ще бъде стартиран като Flask приложение.
                    "FLASK_APP": "app.py",
                    // Задава режима на работа на Flask (разработване).
                    "FLASK_ENV": "development"
                },
                // Конзолата, в която ще се показват изходните данни от програмата.
                "console": "integratedTerminal"
            },
            {
                // Тип на конфигурацията, в този случай за Node.js.
                "type": "node",
                // Режим на стартиране на дебъг сесията.
                "request": "launch",
                // Име на конфигурацията, което се показва в списъка с конфигурации.
                "name": "Launch JavaScript File",
                // Програмата, която ще бъде стартирана (определен JavaScript файл).
                "program": "${workspaceFolder}/your_js_file.js",
                // Пропускане на вътрешни модули на Node.js по време на дебъгване.
                "skipFiles": ["<node_internals>/**"]
            }
        ],
        // Комбинации от конфигурации, които могат да бъдат стартирани заедно.
        "compounds": []
    },
    // Автоматично запазване на файловете след определено забавяне.
    "files.autoSave": "afterDelay",
    // Деактивиране на мини картата в редактора.
    "editor.minimap.enabled": false,
    // Модификатор за мулти курсор, използван в редактора.
    "editor.multiCursorModifier": "ctrlCmd",
    // Автоматично форматиране на файла при запазване.
    "editor.formatOnSave": true,
    // Увеличаване на размера на шрифта при използване на скролера на мишката.
    "editor.mouseWheelZoom": true,
    // Схеми за валидиране и автозавършване на JSON файлове.
    "json.schemas": [],
    // Профил за отваряне на нови прозорци.
    "window.newWindowProfile": "Default",
    // Потребителски думи за spell checker-a.
    "cSpell.userWords": [
        "Venci" // добавя думата "Venci" към речника на spell checker-a.
    ],
    // Разрешава linting за Python файлове.
    "python.linting.enabled": true,
    // Разрешава използването на pylint за Python файлове.
    "python.linting.pylintEnabled": true,
    // Разрешава използването на flake8 за Python файлове.
    "python.linting.flake8Enabled": true,
    // Определя форматиращия инструмент за Python (Black).
    "python.formatting.provider": "black"
}
